# Packer 环境变量配置示例
# 复制此文件为 .env 并填入实际值：cp .env.example .env
# 使用前加载环境变量：source .env

# ============================================
# Proxmox API 连接信息
# ============================================
# Proxmox API URL（包含端口和路径）
export PROXMOX_API_URL="https://192.168.1.100:8006/api2/json"

# Proxmox API Token ID（格式：用户@realm!token_name）
export PROXMOX_API_TOKEN_ID="terraform@pve!coder"

# Proxmox API Token Secret
export PROXMOX_API_TOKEN_SECRET="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件：
#    cp .env.example .env
#
# 2. 编辑 .env 文件，填入实际的 Proxmox API 凭据
#
# 3. 加载环境变量：
#    source .env
#
# 4. 运行 Packer：
#    packer build -var-file="config.pkrvars.hcl" ubuntu-24.04.pkr.hcl
#
# 注意：.env 文件已在 .gitignore 中排除，不会被提交到 git
